[project]
name = "demo"
version = "0.1.0"
description = "Demo of config plane functionality"
requires-python = ">=3.12"
dependencies = [
    "config-plane",
    "loguru>=0.7.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

